# service_caco

CACO Calculator for HLS peers to estimate baseline credit impact from workload increases.

## Usage

Open `index.html` in a browser and enter:

- Current baseline credits
- Workload increase percentage

The calculator returns:

- Recommended new baseline
- Monthly incremental credit
- Annualized incremental credit
- A shareable guidance summary for internal/customer conversations
